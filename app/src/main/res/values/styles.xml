<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="btnMenuBottom">
        <item name="android:textColor">@drawable/tab_text_color</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">10dp</item>
    </style>

    <style name="DayTheme" >
        <item name="colorValue">@color/colorTopbarBg</item>
        <item name="floatValue">0.35</item>
        <item name="integerValue">33</item>
        <item name="booleanValue">false</item>
        <item name="dimensionValue">16dp</item>
        <item name="itemReferenceValue">@color/colorWhite</item>
        <!-- 如果string类型不是填的引用而是直接放一个字符串,在布局文件中使用正常,但代码里获取的就有问题 -->
        <!--<item name="stringValue">@string/action_settings</item>-->
        <item name="referenceValue">@color/colorBg</item>
        <item name="imageValue">@mipmap/icon_day</item>
        <item name="curVisibility">show</item>
    </style>

    <style name="NightTheme" >
    <item name="colorValue">@color/colorWhiteAlpha</item>
    <item name="floatValue">1.44</item>
    <item name="integerValue">55</item>
    <item name="booleanValue">true</item>
    <item name="dimensionValue">18sp</item>
    <item name="itemReferenceValue">@color/colorBlack</item>
    <!--<item name="stringValue">@string/night_action_settings</item>-->
    <item name="referenceValue">@color/colorTopbarBg</item>
    <item name="imageValue">@mipmap/icon_night</item>
    <item name="curVisibility">inshow</item>
    </style>
</resources>
